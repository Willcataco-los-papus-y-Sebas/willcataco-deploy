apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend-dev
  template:
    metadata:
      labels:
        app: backend-dev
    spec:
      containers:
      - name: backend
        image: ghcr.io/willcataco-los-papus-y-sebas/willcataco-server:dev-d7b19b965d06762a4e3da4d8dc588d578f8ed3ab
        ports:
        - containerPort: 8000
        envFrom:
        - secretRef:
            name: backend-secrets-dev
        env:
        - name: DB_HOST
          value: postgres-dev
        - name: DB_PORT
          value: "5432"
